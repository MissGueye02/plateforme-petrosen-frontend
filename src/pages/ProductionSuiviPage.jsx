import { useEffect, useState } from "react";
import Topbar from "../components/Topbar";
import { getProductionStats, getProductionSuivi } from "../services/fieldServices";

export default function ProductionSuiviPage(){const[rows,setRows]=useState([]);const[stats,setStats]=useState({});const[error,setError]=useState("");const refresh=async()=>{try{const[a,b]=await Promise.all([getProductionSuivi(),getProductionStats()]);setRows(a?.data||[]);setStats(b||{})}catch(e){setError(e.message)}};useEffect(()=>{refresh()},[]);return <div className="app-shell"><Topbar/><main className="page-wrap"><div className="page-heading"><div><span className="eyebrow">Suivi</span><h1>Production</h1><p>Consultation des données saisies par les ingénieurs.</p></div></div><section className="dashboard-stats"><div className="stat-card"><span className="label">Productions</span><div className="value">{stats.nb_productions||0}</div></div><div className="stat-card"><span className="label">Volume total</span><div className="value">{Number(stats.volume_total||0).toFixed(2)} m³</div></div><div className="stat-card"><span className="label">Pression moyenne</span><div className="value">{stats.pression_moyenne ? `${stats.pression_moyenne} bar` : "—"}</div></div></section><div className="page-card">{error&&<div className="alert-box danger">{error}</div>}<div className="table-card table-responsive"><table className="table"><thead><tr><th>Date</th><th>Puits</th><th>Bloc</th><th>Volume</th><th>Pression</th><th>Niveau</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td>{p.date_production}</td><td>{p.puits?.nom||"—"}</td><td>{p.puits?.bloc_petrolier?.nom||"—"}</td><td>{p.volume} m³</td><td>{p.pression} bar</td><td><span className={p.niveau==="anormal"?"status-pill danger":"status-pill"}>{p.niveau}</span></td></tr>)}</tbody></table></div></div></main></div>}
